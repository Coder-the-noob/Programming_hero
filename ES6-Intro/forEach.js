const numbers = [1, 2, 3, 4, 5];
numbers.forEach(num => {
    console.log(num * 2);
});

// Use forEach when you just want to do something with each element (e.g., printing, saving, updating external variables).