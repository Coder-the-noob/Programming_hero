const numbers = [9, 35, 56, 23, 31, 67];

const firstEven = numbers.find(num => num % 2 === 0);

console.log(firstEven);

// Use find when you want to retrieve the first element that matches a certain condition.